# Block diagram — affinity_filter (M4b)

The affinity stage is documented together with the relation stage as one **filter pipeline** in
[`relation_filter.md`](relation_filter.md) (they form a single per-channel-class pipeline; the affinity
stage is a neutral identity in the MVP, `valence_gain=0`). See that file for both stages and the
signal schematic.
