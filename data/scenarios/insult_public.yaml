# Public insult (spec section D: public_vs_private_insult). social_exposure amplifies the
# relational channel, so outburst_potential[public] > [private]. Pair with insult_private.
# Run across personas: Wojslaw -> outburst, Halgrim -> cold_response (same insult).
id: insult_public
persona: wojslaw
initial_overrides: {}
events:
  - {type: insult, t: 0, source: player, intensity: 1.0, context: {public: true}}
