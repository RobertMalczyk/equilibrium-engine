# GENERATED by calibration/calibrate_burst.py — do not hand-edit (provenance record).
# Read ON TOP OF defaults.yaml; defaults stay neutral/placeholder. Wired into the eval
# loader at plan step 4 (burst overlay, opt-in) — until then this is a provenance artifact.

layer: burst (M20.1)
reads_on_top_of: calibrated_layer2.yaml + calibrated_recovery.yaml + defaults.yaml (all untouched)
anchor: the frozen Layer-2 2-cycle Jury bound + the measured corpus operating-point envelope; every burst
  number is expressed RELATIVE to these, none chosen by hand
free_set:
- coupling_escalation.anger.stress
- coupling_escalation.stress.anger
calibrated:
  coupling_escalation.anger.stress:
    value: 1.1
    kind: k_esc
    status: calibrated-C1
    provenance: 'C1 feasibility frontier (calibration/calibrate_burst.py). Frozen Layer-2: bound=(1-d_s)(1-d_a)=0.002847,
      g_as*g_sa=0.000914, max escalation factor R=3.1146. Measured envelope (eval/burst_operating_points.json):
      max frequent <=2-way pair (anger=0.700, stress=0.526); >=3-way operating points do NOT exceed the
      pair ceiling, so k_esc cannot gate on coincidence count (the latch band does). Feasible window [k_lo=1.097
      (spiral at band entry anger>=0.8/stress>=0.6), k_hi=1.253 (max observed pair stays stable)]; chose
      the low end for max stability margin at the frequent operating points.'
  coupling_escalation.stress.anger:
    value: 1.1
    kind: k_esc
    status: calibrated-C1
    provenance: symmetric with coupling_escalation.anger.stress (no data to split the two edges; one shared
      k, the absolute anchor for the burst layer)
band_entry_used_for_C1:
  anger: 0.8
  stress: 0.6
stages_pending:
- C2 burst_extinction (return-and-stay within T_cool)
- "C3 latch geometry (theta_burst_enter/exit/confirm \u2014 pins BAND_ENTRY)"
- C4 Loop-2 sign (urge_boredom.stress, seek stress-cost)
- C5 theta_displace + displaced discount
- refractory edge weight (potential_weights.outburst.refractory_x_resent_src)
