# GENERATED by calibration/calibrate_burst.py — do not hand-edit (provenance record).
# Read ON TOP OF defaults.yaml; defaults stay neutral/placeholder. Wired into the eval
# loader at plan step 4 (burst overlay, opt-in) — until then this is a provenance artifact.

layer: burst (M20.1)
reads_on_top_of: calibrated_layer2.yaml + calibrated_recovery.yaml + defaults.yaml (all untouched)
anchor: the frozen Layer-2 2-cycle Jury bound + the measured corpus operating-point envelope; every burst
  number is expressed RELATIVE to these, none chosen by hand
free_set:
- coupling_escalation.anger.stress
- coupling_escalation.stress.anger
- burst_extinction.anger
- burst_extinction.stress
- thresholds.burst_enter.anger
- thresholds.burst_enter.stress
- thresholds.burst_exit
- thresholds.burst_confirm_ticks
- action_params.seek_stimulus.per_tick.stress
- derived_weights.urge_boredom.stress
- thresholds.theta_displace
- appraisal.displaced_relational_discount
- thresholds.refractory_anger
calibrated:
  coupling_escalation.anger.stress:
    value: 1.1
    kind: k_esc
    status: calibrated-C1
    provenance: 'C1 feasibility frontier (calibration/calibrate_burst.py). Frozen Layer-2: bound=(1-d_s)(1-d_a)=0.002847,
      g_as*g_sa=0.000914, max escalation factor R=3.1146. Measured envelope (eval/burst_operating_points.json):
      max frequent <=2-way pair (anger=0.700, stress=0.526); >=3-way operating points do NOT exceed the
      pair ceiling, so k_esc cannot gate on coincidence count (the latch band does). Feasible window [k_lo=1.097
      (spiral at band entry anger>=0.8/stress>=0.6), k_hi=1.253 (max observed pair stays stable)]; chose
      the low end for max stability margin at the frequent operating points.'
  coupling_escalation.stress.anger:
    value: 1.1
    kind: k_esc
    status: calibrated-C1
    provenance: symmetric with coupling_escalation.anger.stress (no data to split the two edges; one shared
      k, the absolute anchor for the burst layer)
  burst_extinction.anger:
    value: 0.043529
    kind: extinction_rate
    status: calibrated-C2
    provenance: 'C2 extinction (calibration/calibrate_burst.py). Absolute anchor = the believable cool-down
      T_cool=1h = 30 ticks at the believable dt. ONE free scalar beta=0.65 with ext_x=beta*(1-decay_x)
      (split inherited from the frozen Layer-2 decays => anger faster than stress, no invented ratio).
      beta is the SMALLEST value whose fully-saturated (1,1) latched cool-down returns below theta_burst_exit=0.4
      within T_cool and stays down (monotone). Result: return at tick 30 (30<= 30), spectral radius at
      the ceiling lambda_max=0.9763<1 (bounded). ext_anger=beta*(1-decay_anger)=0.043529.'
  burst_extinction.stress:
    value: 0.027633
    kind: extinction_rate
    status: calibrated-C2
    provenance: C2 extinction, stress edge. Same scalar beta=0.65; ext_stress=beta*(1-decay_stress)=0.027633
      < ext_anger (0.043529) by construction (stress cools slower than anger, per the design note).
  thresholds.burst_enter.anger:
    value: 0.8
    kind: latch_band
    status: calibrated-C3
    provenance: "latch arm band, anger edge = 0.8. Set just ABOVE the measured frequent <=2-way anger\
      \ ceiling (0.700) and at the escalated-loop (k_esc=1.1) spiral boundary \u2014 the Jury margin is\
      \ ~0 at (0.80,0.60) and negative above, so the loop only spirals (=burst) once both states clear\
      \ the ordinary envelope. Carries the 'rare and earned' selectivity that k_esc cannot (C1 finding:\
      \ >=3-way pairs don't out-reach <=2-way pairs)."
  thresholds.burst_enter.stress:
    value: 0.6
    kind: latch_band
    status: calibrated-C3
    provenance: latch arm band, stress edge = 0.6; same anchor as thresholds.burst_enter.anger (just above
      the measured frequent <=2-way stress ceiling 0.526; both states must be in-band for the loop plateau).
  thresholds.burst_exit:
    value: 0.4
    kind: latch_hysteresis
    status: calibrated-C3
    provenance: 'latch release hysteresis (anger) = 0.4. < enter.anger (0.8) and just below the frequent
      <=2-way anger p99 (0.4426): the latch releases once acute anger has fallen back beneath the ordinary
      reactive ceiling, so it cannot re-arm from a normal provocation. Hysteresis gap 0.40. Shared with
      C2: extinction (beta=0.65) is sized so the fully-saturated cool-down reaches this exit within T_cool=30
      ticks (return at tick 30).'
  thresholds.burst_confirm_ticks:
    value: 2
    kind: latch_dwell
    status: calibrated-C3
    provenance: "latch confirm dwell = 2 ticks. The minimal value > 1: a single in-band tick must not\
      \ arm the latch \u2014 the burst signature is a sustained LOOP plateau (both states in-band), not\
      \ an instantaneous coincidence. 2 ticks ~ a few minutes at the believable dt, short vs the bad-day\
      \ plateau (step-1 benchmarks are plateau-capable). Matches the G4 discrimination tests."
  action_params.seek_stimulus.per_tick.stress:
    value: 0.02
    kind: loop2_seek_cost
    status: calibrated-C4
    provenance: 'C4 Loop-2 forward edge (fruitless looking wears you down). = half the calibrated rich-world
      relief rate |self_activity.stress|=0.040 (deliberate asymmetry: engaging-relief must DOMINATE looking-windup
      so a rich world resolves stress; the cost is ''small''). Mock-world contrast (lutek, STRESSED):
      rich stress slope -0.0285/tick (relief), barren +0.0123/tick (wind-up into burst range); at cost
      0 barren is -0.0036 (no wind-up). The operative Loop-2 knob.'
  derived_weights.urge_boredom.stress:
    value: 0.0
    kind: loop2_return_edge
    status: measured-inert-C4
    provenance: "C4 MEASURED FINDING: the stress->seek return edge is NOT identified by the rich/barren\
      \ contrast \u2014 boredom already drives seeking in every burst-relevant (wound-up) scenario, and\
      \ making a pure-stress (boredom 0) character seek requires w_s ~ the seek threshold (~0.85), a large,\
      \ fragile hand-set value. Per topology-now / never-invent-numbers, the edge stays NEUTRAL (0); Loop\
      \ 2 is carried by the seek stress-cost (B2). The topology edge exists, the weight is 0."
  thresholds.theta_displace:
    value: 0.6
    kind: displacement_bar
    status: calibrated-C5
    provenance: 'displacement bar = 0.6 = midpoint of the C3 latch band [exit 0.4, enter.anger 0.8]. Displacement
      fires only in the DEEP half of the burst: above typical reactive anger (frequent <=2-way anger p99
      0.4426) and below the plateau where the latch arms (0.8); the gate closes as anger cools through
      the band''s lower half, before the latch releases at exit. Validated by the existing G5 displacement
      tests.'
  appraisal.displaced_relational_discount:
    value: 0.0
    kind: displaced_grudge_discount
    status: default-C5
    provenance: "displaced relational discount = 0.0: the discharge onto an INNOCENT bystander books NO\
      \ durable grudge (fully transient \u2014 'snapped at her'); the intended default that excludes the\
      \ fabricated-nemesis runaway. Nonzero only for a story/persona target with a provenance-backed partial\
      \ grudge (none here)."
  thresholds.refractory_anger:
    value: 0.3
    kind: refractory_gate
    status: topology-decoupled
    provenance: 'spent-fury refractory edge (4th inhibitory), DECOUPLED from the vent latch (2026-06-14).
      Arms the brake when a SAME-source re-provocation lands while anger >= 0.3 (the carried heat of a
      recent eruption at that source) -> the spent fury yields to a lower-intensity reply (the term refractory_pressure
      x resentment[src] is read NEGATIVE by outburst; both weights already in defaults). ~2x the calm
      baseline (~0.15): elevated yet below the outburst-trigger level, so it gates the REPEAT not the
      first eruption (a new source). Decoupled because a single relentless provoker is one stable loop
      that never reaches the band -> a latch-gated brake could never help that case.'
stages_pending:
- "refractory edge weight (potential_weights.outburst.refractory_x_resent_src) \u2014 turn ON"
- wire the burst overlay into the eval loader (plan step 4, opt-in)
- 1400-scenario regression + sanity + cichy_multi_060 (plan step 5)
